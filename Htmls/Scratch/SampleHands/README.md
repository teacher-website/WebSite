# 手勢辨識大挑戰（數字 0～4 ＋ 剪刀石頭布）

用 **Handpose2Scratch** 做的兩種手勢判斷教學，含可執行的 `.sb3` 範例與互動式學習網頁。

## 這個專案在教什麼

同一套偵測，做出兩種不同的判斷：

| | 問的問題 | 用到的資料 |
|---|---|---|
| **① 數字關** | 幾根手指伸直？（0～5） | `手指數`（五根相加） |
| **② 猜拳關** | 是哪幾根手指伸直？ | `四指型`（四位密碼 0000／1100／1111，不看拇指） |

五根手指分成兩套判斷，剛好是很好的教學對照 ——
**直向的比較用直向的尺，橫向的比較用橫向的尺**：

> **食指～小指**（往「上」伸）
> 指尖的 y **＞** 同一根手指第二關節的 y　→　伸直
>
> **拇指**（往「旁邊」伸）
> ｜拇指尖 x − 食指第三關節 x｜ **＞** 手寬 × 0.45　→　伸直
> （手寬 ＝ ｜食指第三關節 x − 小指第三關節 x｜）

只用到比大小、加法、絕對值與一個比例，不需要平方根或角度；
用「手長」「手寬」當尺，所以手離鏡頭遠近不影響；取絕對值，所以左右手、鏡不鏡像都不用管。

實際要在教室裡穩定運作，還加上四道保險（教學第 5 章有實測數字）：

1. **守門 ①**　手腕 x > −250 —— 偵測不到手時座標積木回傳空字串
2. **守門 ②**　中指第三關節 y > 手腕 y —— 確認手有立起來
3. **每根手指的餘裕（遲滯）**　餘裕 ＝ 手長 × 0.08，要超過才改變狀態，中間維持原狀
4. **整組手勢的穩定時間**　同一個指型維持 0.15 秒以上才寫進「確定」變數
5. **掉格容忍**　連續 0.4 秒抓不到手才清空 —— 少了這一道，攝影機偶爾漏一格就會讓玩家覺得毫無反應

## 目錄結構

```
20260924_手勢辨識_數字與猜拳_Web/
├─ index.html                 教材入口
├─ README.md
├─ lint_costume.py            靜態掃描：抓「造型換成 (數字)」這類問題
└─ student/
   ├─ index.html              製作教學（10 章 ＋ 4 個互動實驗室 ＋ 試玩台）
   ├─ doc.html                程式說明文件（辨識方法、變數表、可調數值表）
   ├─ 手勢辨識大挑戰.sb3       範例檔（18 角色 / 862 積木）
   └─ assets/                 style.css · anim.css · hand.css · blk.js · lesson.js
```

## 怎麼用

1. 用瀏覽器打開 `index.html`。
2. 範例檔請用 **stretch3.github.io** 開啟，左下角「添加擴展」選 **Handpose2Scratch**，允許使用攝影機後點綠旗。
3. 沒有攝影機也能上課：按 **K** 切換鍵盤模式，之後按 **0～4** 就等於比出手勢。

### 鍵盤快速鍵

| 鍵 | 作用 |
|---|---|
| `1` / `2` | 在選單時直接進入數字關 / 猜拳關 |
| `M` | 回選單 |
| `D` | 開關骨架點 |
| `K` | 切換鍵盤模式 |
| `0`～`5` | 鍵盤模式下代替手勢 |

## 教學設計重點

- **第 2 章**：為什麼是「指尖 vs 同一根手指的關節」，而不是絕對座標、指尖距離或角度。
- **第 3 章**：拇指換個方向，就要換一把尺。這是整份教材裡最值得討論的一段。
- **第 4 章**：讓學生自己發現「數字 2」和「剪刀」都是兩根 —— 數量不夠用，要看指型。
  猜拳刻意**不看拇指**，因為比剪刀時拇指收或翹因人而異。
- **第 5 章**：四道保險與實測數字。兩個關鍵觀念：
  防抖動要用**計時器**（維持 0.15 秒）而不是次數 —— Scratch 的 `重複無限次`
  若內容只有變數運算，一個畫面更新內會跑好幾百次，「連續 5 次」等於沒有防到；
  以及**抓不到手時不要馬上清空**，否則手指燈是對的、遊戲卻怎麼都不認。
- **第 7 章**：三個坑。出題要避開玩家**現在**比的數字；等待要用「重複直到（比對了 **或** 超過 8 秒）」；
  以及 **`造型換成 (題目)` 會顯示錯的數字** —— 輸入是數字時 Scratch 會當成「第幾個造型」，
  必須用字串組合變成文字。
- **第 8 章**：猜拳勝負可以用一個餘數算式取代三條規則 ——
  `（我的編號 ÷ 3 的餘數）＋ 1 ＝ 對方編號 → 我贏`（1 石頭、2 剪刀、3 布）。

## 驗證紀錄

範例檔以 headless `scratch-vm` 實際載入執行，通過下列測試：

- **32 種**手指組合（含拇指）的 `手指數` / `指型` / `四指型` / `猜拳號` 全部正確
- 拇指：張開時橫向距離為手寬的 0.97 倍、收起時 0.22 倍，門檻 0.45 落在中間
- 手縮放到 0.35～1.6 倍（手寬 18.9～86.4）→ 拇指判斷完全一致
- 猜拳忽略拇指：剪刀／布 在拇指收或翹時都判對；拇指翹的拳頭仍判為石頭
- 偵測不到手、手橫放 → 守門成立（`看得到手 = 0`）
- 掉一格（0.25 秒）→ `確定手指數` 不被清掉；真的離開 0.95 秒 → 歸零、手指燈熄滅
- 同一手勢在三種遠近位置 → 指型一致
- 高速抖動（每 60 ms 換一次）→ `確定指型` 不跟著跳
- **雜訊測試**（每 33 ms 送一組帶雜訊的假座標）：
  雜訊 ±5 px、彎曲指尖僅低於關節 4 px、掉格率 0～20% 的各種組合下，
  `確定手指數` 正確的時間比例皆為 **100%**（修正前為 0～64%）
- **換手勢測試**：連續 10 個手勢，在雜訊 ±6 px、掉格率 15% 下全部認出，
  反應時間中位數 **190 ms**、最慢 289 ms
- **畫面一致性測試**：一支「照題目卡上顯示的數字比手勢」的機器人玩完整局 → **8 / 8 分**
  （這是用來抓「畫面顯示的題目 ≠ 程式裡的題目」這類 bug 的測試）
- **靜態掃描**：`lint_costume.py` 檢查全專案所有動態切換造型的輸入都保證是文字
- 數字關：全部答對得 8 分；完全不動則 8 題都時間到、得 0 分；出題不連續重複
- 猜拳關：15 回合的勝負判定與餘數算式完全一致；分數等於贏的次數
- 猜拳關：一直比看不懂的手勢 → 回合不計，連續 6 次自動回選單
- 關卡中按 M → 回選單且關卡程式確實停止
- 教學網頁：四個互動實驗室與試玩台皆無 JavaScript 例外

## 變更紀錄

- **2026-09-24（第四版）**　**加入拇指，數字擴充到 0～5**。
  - 拇指改用**橫向距離**判斷：`｜拇指尖(5) x − 食指第三關節(6) x｜ > 手寬 × 0.45`，
    手寬 ＝ `｜食指第三關節(6) x − 小指第三關節(18) x｜`。同樣加上遲滯（餘裕 ＝ 手寬 × 0.08）。
  - `指型` 改為五位（拇食中無小）；新增 `四指型`（不含拇指）供猜拳使用 ——
    因為比剪刀時拇指收或翹因人而異，納入判斷會大幅增加「看不懂」。
  - 手指燈 4 → 5 個分身；骨架 9 → 12 個點（新增拇指尖與兩個掌根點，造型改由「點色」清單決定）；
    題目卡新增 `數5`；鍵盤模式新增 `5`。
  - 新增 `thumb.js` 測試：32 種組合、拇指量測值、四種手大小的一致性、猜拳忽略拇指、
    拇指開↔收的反應速度（中位數 200 ms）。
  - 積木數 804 → 862。教學第 0～6、10 章與程式說明文件同步更新，互動實驗室改為五根手指。
- **2026-09-24（第三版）**　修正「數字關怎麼比都不過關」的**真正主因**。
  症狀看起來完全像偵測失效（手指燈正確、確定手指數也正確），實際上是顯示問題：
  題目卡寫成 `造型換成 (題目)`，而 Scratch 在輸入為**數字**時會把它當成
  **「第幾個造型」而不是造型名稱** —— 題目是 3 會換到第 3 個造型（名稱「2」），
  畫面上的數字**永遠少 1**；題目是 0 更會跑到最後一個造型「4」。
  玩家照畫面比，程式當然永遠不認。
  修正：造型改名為 `數0`～`數4`，並用 `造型換成 (字串組合「數」與 題目)`。
  同時補上兩個新測試：① 機器人**照畫面上的造型**作答（不再只讀變數），玩完整局得 8/8 分；
  ② `lint_costume.py` 靜態掃描所有動態切換造型的輸入。
  教學第 7 章與程式說明文件新增這個坑的說明。
- **2026-09-24（第二版）**　修正「手指燈是對的，遊戲卻判定不到」的問題。
  用帶雜訊的假座標重現後找到兩個原因：
  1. 攝影機偶爾掉一格就把 `確定手指數` 清成 −1 並重置穩定計時 →
     只要每 20 格掉 1 格，正確率就從 100% 掉到 61～64%。
     改成連續 0.4 秒抓不到才清空（新增自訂積木「清空手勢」）。
  2. 彎曲手指的指尖離第二關節太近，雜訊一來就在 0／1 之間跳（每秒 19 次），
     0.2 秒的穩定窗永遠湊不滿 → 正確率只有 17%。
     改成每根手指加上遲滯（餘裕 ＝ 手長 × 0.08）。
  同時把穩定門檻從 0.2 秒降到 0.15 秒、監看器加上 `確定手指數`。
  修正後在所有測試條件下正確率皆為 100%，換手勢反應時間中位數 190 ms。
  積木數 731 → 803。教學第 3、5 章與程式說明文件同步更新，
  第 5 章的互動實驗室改成三條軌跡（只比大小／加餘裕／再加穩定時間）。
- **2026-09-24（初版）**　`.sb3`（18 角色 / 731 積木）、10 章教學網頁、程式說明文件。
